#include "edge_tts/communication/Communicate.hpp"

#include <iostream>

int main(int argc, char** argv) {
    (void)argc;
    (void)argv;
    std::cout << "edge-tts placeholder CLI\n";
    std::cout << "Planned options: --text, --file, --voice, --list-voices, --rate, --volume, --pitch, --write-media, --write-subtitles, --proxy\n";
    return 0;
}
